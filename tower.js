
// Main module for importing for full use:

// Importing most common modules,
const { tower } = require('./Floors.js');
const utils = require('./utilities.js');

// and exporting it for global use:
module.exports = { tower, utils };
